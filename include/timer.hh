#ifndef TIMER_HH
#define TIMER_HH

#include <memory>
#include <chrono>
#include <utility>
#include <thread>
#include <iostream>

namespace time_bench
{
    class Timer
    {
    private:
        /* data */
    public:
        Timer(/* args */);
        ~Timer();
    };

} // namespace timer

#endif // TIMER_HH
